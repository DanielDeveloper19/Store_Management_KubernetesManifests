apiVersion: v1
kind: Service
metadata:
  name: {{ include "storeManagement_chart.fullname" . }}
  labels:
    {{- include "storeManagement_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "storeManagement_chart.selectorLabels" . | nindent 4 }}
