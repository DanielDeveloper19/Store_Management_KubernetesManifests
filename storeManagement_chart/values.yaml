# Default values for Store_management.
replicaCount: 2

image:
  repository: ghcr.io/DanielDeveloper19/Store_management
  pullPolicy: IfNotPresent
  # The CI pipeline will automatically update this tag value
  tag: "c406403"

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 200m
    memory: 256Mi